# Example targets file for `sv2-compliance --config targets.toml`
#
# Each [[site]] is one deployment.  Only the addresses you provide are tested:
#   pool_address -> tested as a solo pool (Mining Protocol)
#
# `user_identity` (optional) is used when opening mining channels.

[[site]]
name = "SRI Community SoloPool"
pool_address = "75.119.150.111:3333"
